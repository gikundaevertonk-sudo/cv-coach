import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the on-screen Next.js dev badge (bottom-left). Compile and runtime
  // errors are still surfaced. Remove this line to bring the indicator back.
  devIndicators: false,
};

export default nextConfig;
